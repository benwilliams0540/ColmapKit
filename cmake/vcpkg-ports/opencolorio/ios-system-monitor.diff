diff --git a/src/OpenColorIO/SystemMonitor.cpp b/src/OpenColorIO/SystemMonitor.cpp
--- a/src/OpenColorIO/SystemMonitor.cpp
+++ b/src/OpenColorIO/SystemMonitor.cpp
@@ -2,6 +2,10 @@
 // Copyright Contributors to the OpenColorIO Project.
 
 
+#if defined(__APPLE__)
+#include <TargetConditionals.h>
+#endif
+
 #include <sstream>
 #include <string>
 #include <string.h>
@@ -13,7 +17,8 @@
 #include "SystemMonitor.h"
 
 
-#ifdef OCIO_HEADLESS_ENABLED
+#if defined(OCIO_HEADLESS_ENABLED) || \
+    (defined(__APPLE__) && TARGET_OS_IPHONE)
 
 namespace OCIO_NAMESPACE
 {
diff --git a/src/OpenColorIO/CMakeLists.txt b/src/OpenColorIO/CMakeLists.txt
--- a/src/OpenColorIO/CMakeLists.txt
+++ b/src/OpenColorIO/CMakeLists.txt
@@ -332,7 +332,7 @@ if(OCIO_USE_SIMD AND OCIO_USE_SSE2NEON AND COMPILER_SUPPORTS_SSE_WITH_SSE2NEON)
     target_link_libraries(OpenColorIO PRIVATE $<BUILD_INTERFACE:sse2neon>)
 endif()
 
-if(APPLE)
+if(APPLE AND NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
     target_link_libraries(OpenColorIO
         PRIVATE
             "-framework ColorSync"
